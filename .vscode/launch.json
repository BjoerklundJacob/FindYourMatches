{
    "version": "0.2.0",
    "configurations": [
        {
            "command": "npm start",
            "name": "Run npm start",
            "request": "launch",
            "cwd": "${workspaceFolder}/find-your-matches",
            "type": "node-terminal"
        }
    ]
}